import { AreaSearch, IconSearch, Input } from "./styles";

export const InputSearch = () => {
  return (
    <AreaSearch>
      <Input placeholder="Busca" />
      <IconSearch />
    </AreaSearch>
  );
};
